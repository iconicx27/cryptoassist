import 'package:flutter/material.dart';

class AssetData extends StatelessWidget {
  const AssetData({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('AssetPage'),
    );
  }
}
