import 'package:flutter/material.dart';

class FavouriteData extends StatelessWidget {
  const FavouriteData({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("FavouritePage"),
    );
  }
}
